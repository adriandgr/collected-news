const articleKeywords = require('./articlekeywords');
const articles = require('./articles');
const keywords = require('./keywords');
const keywordSources = require('./keywordsources');
const sources = require('./sources');

module.exports = {
  articleKeywords,
  articles,
  keywords,
  keywordSources,
  sources,
};
